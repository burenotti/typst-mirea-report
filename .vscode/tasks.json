{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [ 
        {
            "label": "build-reports",
            "group": "build",
            "type": "shell",
            "command": "bash ./.vscode/compile_all.sh \"${workspaceFolder}\"",
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ]
}